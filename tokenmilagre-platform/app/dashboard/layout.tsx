// Dashboard layout simplificado - navegação unificada agora está no layout root
export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
